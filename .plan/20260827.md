# SocietyAdvisor · 今日开发计划 · 2026-08-27

> T4 应急补位(原 T4 02:00 未生成 .plan,3 日连续缺失已升级风险)· T1 03:20 接管
> 巡检上下文:Phase 0 2/6,Phase 1 0/6,`社会顾问开发架构与计划.md` 连续 3 日 untracked

## 今日目标(1 个小变更)

**Phase 0 #3:议题关系抽取(最小版本)**

- 产出一个 `data/issue_relations.yaml`(8-12 条手工标注的"原因/影响/相关"边)
- 配一个 `data/issue_relations.schema.md` 字段规范
- 扩展 `scripts/md_to_json.py` 支持 `relations` 子命令/参数,生成 `data/issue_relations.json`
- 跑通脚本,验证 schema

## 验收标准

- `data/issue_relations.yaml` ≥ 8 条边,所有 `source_id` / `target_id` 在 issues.yaml 存在
- `data/issue_relations.schema.md` 字段、枚举、长度约束清晰
- `scripts/md_to_json.py relations` 子命令可跑,生成 JSON 包含 `meta` + `relations` 两块
- 1 个 checkbox 翻转:`项目开发计划.md` Phase 0 议题关系抽取 `- [ ]` → `- [x]`
- README.md 状态 2/6 → 3/6

## 不做

- 不动 `社会顾问开发架构与计划.md`(巡检项,本轮继续 untracked)
- 不动 FastAPI 骨架 / 飞书日报 cron(留给后续 T5)
- 不重排 issues.yaml(本期只加 relations 层,不动原表)
