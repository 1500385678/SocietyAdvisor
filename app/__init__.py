"""SocietyAdvisor API package.

Phase 0 #5: FastAPI 骨架 + /graph /pulse 最小可用接口。
数据源:data/issues.yaml + data/issue_relations.yaml(由 scripts/md_to_json.py 同源生成 JSON)。
启动:uvicorn app.main:app --reload --port 8000
"""
__version__ = "0.1.0"
